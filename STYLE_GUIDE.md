# Repository Style Guide

本文件保存论文整理时容易丢失的格式和术语约定。新增或修改 `daily/`、`conferences/`、`domains/` 前先读取本文件，再执行对应目录 README 中的 Prompt。

## Language

1. 论文标题使用英文原题，不自行翻译或改写。
2. 关键词统一使用英文，每篇 3 至 4 个，以中文顿号 `、` 分隔。第一个关键词固定表示研究角色：`attack`、`defense`、`detection`、`analysis`、`survey`、`benchmark` 或 `tool`。
3. method、attack、defense、benchmark、dataset、model、metric 和 system 名称保留论文原文及原始大小写，例如 `PoisonedEvolution`、`GraphRAG`、`clean-label attack`、`chain-of-thought` 和 `ASR`，不要为了全文中文而硬译。
4. 研究方向和一句话总结以中文组织，但可以直接保留准确的英文专有名词；同一个术语在同一文件中保持同一写法。
5. 不使用来源没有提出的宣传性形容词，也不把实验结果扩写为更广泛的安全结论。

## Domain Pages

叶子领域文件使用以下结构：

```markdown
# 领域名称

[返回上级目录](README.md)

## 研究方向

一段说明研究对象、主要 threat model、攻击面、方法与评测边界的文字。

## 研究脉络

- **问题起点：** 说明该方向最初解决的问题或建立的基本 threat model。
- **方法演进：** 说明攻击、分析或方法如何扩展。
- **检测与防御：** 说明相应评测、防御或机制研究的发展。
- **当前边界：** 说明仍未解决的问题；没有可靠依据时不强行添加。

## 攻击、检测、防御或该领域特有的研究分支

| 时间 | 论文名称 | 关键词 | 会议中稿情况 | 论文链接 | 代码链接 | 一句话总结 |
| --- | --- | --- | --- | --- | --- | --- |
```

1. 页面不能只堆一张不分语义的大表。优先按该领域真实研究问题拆成独立小节和表格，例如“攻击”“检测与溯源”“防御与移除”“机制分析”；攻击较多时继续按攻击面、模型类型或方法路线细分。
2. `Survey`、`Benchmark` 和可复用的基础 `Tool` 单独成节，不混在普通攻击或方法表里。领域确实只有一种研究角色时可以只保留一个表，但小节标题仍需指出具体研究路线。
3. `研究脉络` 使用 3 至 5 个无序列表项，按时间阶段或并行方法路线组织；每项以 `**阶段标签：**` 开头，只表达一个阶段或一条路线，不写成长段。标签应贴合具体领域，不必机械复用模板中的名称。
4. 关键词采用“研究角色 + 领域对象 + 核心机制 + 影响或评测维度”的槽位，而不是给所有页面套同一组词。例如 RAG 使用 `attack、GraphRAG poisoning、topology poisoning、knowledge corruption`，VLA 使用 `attack、VLA backdoor、state-space trigger、action hijacking`。
5. 后三个关键词必须根据当前 domain 选择最有区分度的术语；避免 `LLM security`、`AI safety` 等无法说明论文内容的泛词，也避免 `GraphRAG poisoning、GraphRAG` 这类同义或上下位重复。
6. 时间使用 `YYYY&#8209;MM`，让渲染后的 `YYYY-MM` 不换行；月份无法确认时只写 `YYYY`。
7. 每个分表内部按时间从近到远排列；同月内也优先排列提交时间较新的论文。只有年份的条目放在该年所有已知月份之后。
8. 不在标题、README 或正文统计论文数量。
9. 一句话总结按“问题出发点；做了什么；主要结论”组织为一句话，保留关键 metric 和 method name。
10. 核心领域的 README 只维护范围与子目录，不放论文表；论文只进入最匹配的叶子 `.md`。
11. 从 `daily/` 或 `conferences/` 同步时，先用 arXiv ID、再用规范化英文标题去重；会议官方链接与 arXiv 可以并列保留，但不得因此生成两条记录。
12. 同步来源中的关键词和研究角色只作为候选信息；若它们与标题、摘要或官方记录冲突，应依据论文内容重新判断，不能把相邻行的错误标签传播到领域页。
13. 新领域必须对应稳定且可描述的 threat model、方法和评测边界；只有单篇工作或仅有泛化相似词时，优先归入最接近的现有页面或保留待分类。
14. 少量重点论文可以使用 `domains/README.md` 定义的可见标记；标记只追加在标题原文之后，不属于论文标题，按 arXiv ID 或规范化标题去重时必须忽略。

## Links And Venues

1. 论文链接优先使用官方 proceedings、OpenReview forum 或 arXiv abstract page，不使用搜索结果页和无来源转载。
2. 会议状态只有在会议官网、正式 proceedings 或 OpenReview decision 可确认时才填写；arXiv 预印本不能视为录用。
3. 未确认 venue 的 arXiv 论文写 `未注明（arXiv）`；明确投稿但未录用时如实写明，不省略负面 decision。
4. 代码必须是作者或论文明确提供的 repository；使用 `[Code](URL)`。只有 project page 时写 `[Project](URL)`，未找到时写 `暂未公开`。
5. 论文声明已公开代码但链接尚未核实时写 `论文声明公开，链接待核实`，不能猜测 repository URL。
6. 官方 repository 已建立但尚无代码文件时写 `[Repository](URL)（当前仅 README）`，不能将占位仓库标为 `[Code]`。

## Research Blogs

领域页存在与研究脉络直接相关的一手解读时，在论文小节之后追加：

```markdown
## 相关研究博客

| 时间 | 标题 | 发布机构或作者 | 关联主题 | 链接 | 核心内容 |
| --- | --- | --- | --- | --- | --- |
```

1. 优先收录论文作者的 technical post、研究机构官方 research blog 或项目团队的正式说明；不收录新闻转载、SEO 汇总和只改写 abstract 的二手内容。
2. Blog 与论文表分开记录。已经进入论文表的工作仍可收录对应 blog，但“核心内容”要说明 blog 额外提供的直观解释、实验细节、部署语境或 limitation，不能重复论文的一句话总结。
3. `关联主题` 使用 2 至 3 个有区分度的英文术语，以中文顿号分隔；不需要添加论文表所要求的研究角色前缀。
4. 时间使用 `YYYY&#8209;MM`，每个 blog 表内部从近到远排序；发布日期无法核实时不收录。
5. 同一研究同时有面向公众的机构文章和更技术化的作者文章时，可在同一条目的链接列并列给出，避免把同一结果拆成重复记录。

## Daily And Conference Files

1. 日报保持列表形式，不改成表格。每篇论文在标题后先写不带列表符号的 `**内容概述：**` 中文自然段，再写“收录理由”，之后才列链接、arXiv 元数据、作者单位和中英文摘要；内容概述压缩为一句话，按“研究动机或前人缺口 → 作者提出的方法或 benchmark → 最关键结果或失效边界”组织，不与收录理由重复。
2. 英文 abstract 使用 arXiv 原文，中文 abstract 忠实完整翻译；内容概述不能替代或删减这两项可核验信息。
3. 会议文件直接保存为 `conferences/<conference-id>_<year>.md`，使用小写 snake_case，不为会议或年份创建子目录。
4. 会议文件依次包含“目录”“会议信息”“关键节点”“筛选说明”“论文分类”和“核验记录”；会议信息必须给出官网、官方录用列表或 proceedings、举办时间与地点、检查范围和数据截至日期。
5. DDL、通知、camera-ready 与会议日期必须来自官方页面，使用 `YYYY-MM-DD`；保留来源标注的 AoE、UTC 或当地时区，来源只给出月份或日期范围时不得补猜。
6. 会议论文不能堆入一张大表。根据该届实际内容按具体安全问题拆成若干小节和小表；分类只服务于当前会议的可读性，不读取或匹配 `domains/`。一篇论文只进入最匹配的一个分表，每个分表按英文标题字母序排列。
7. 所有会议分表统一使用 `Title | 作者 | 论文链接 / arXiv 链接 | 代码链接 | 关键词 | 一句话总结` 六列；Title 与作者顺序服从官方记录，关键词仍按本文件的英文规则填写。
8. 每整理完一个会议立即核验该文件，不等待其他会议完成：逐篇检查官方录用证据、标题、作者、摘要、链接、代码、分类与去重，并在“核验记录”写明日期、覆盖范围和未决项。
9. 日报、会议和领域页各自去重；同一论文因研究问题交叉可以进入多个不同叶子领域。
